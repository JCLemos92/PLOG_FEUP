erro(integer_overflow).
erro(divisao_por_zero).
erro(id_desconhecido).

cod(1,integer_overflow).
cod(2,divisao_por_zero).
cod(3,id_desconhecido).

% basicamente é um if codigo return x erro pode ser melhorado usando a expressão acima